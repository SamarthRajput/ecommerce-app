{
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "scripts": {
    "all": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\" \"cd backend && npx prisma studio\""
  }
}
